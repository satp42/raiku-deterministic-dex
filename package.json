{
  "name": "fairlane",
  "version": "0.1.0",
  "private": true,
  "description": "Deterministic batch auction DEX on Solana with Raiku",
  "scripts": {
    "dev": "pnpm -C apps/web dev",
    "build": "pnpm -r build",
    "lint": "pnpm -r lint"
  },
  "engines": {
    "node": ">=18.0.0",
    "pnpm": ">=8.0.0"
  },
  "packageManager": "pnpm@8.15.0"
}

